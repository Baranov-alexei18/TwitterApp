import { ReactNode } from 'react';

export type ButtonProps = {
  background: string;
  borderRadius: string;
  width: string;
  height: string;
  borderColor: string;
  color: string;
  fontSize: string;
  children: ReactNode;
};
