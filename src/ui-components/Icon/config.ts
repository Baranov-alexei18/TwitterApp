import { BORDER_RADIUS, SPACING } from '@/theme/variables';

export const StyledIcon40 = {
  width: '40px',
  height: '40px',
  margin: `${SPACING.zero} ${SPACING.zero} ${SPACING.xl} ${SPACING.zero}`,
};

export const StyledIconCircle40 = {
  ...StyledIcon40,
  radius: `${BORDER_RADIUS.circle}`,
};

export const StyledIcon24 = {
  width: '24px',
  height: '24px',
  margin: `${SPACING.zero} ${SPACING.sm} ${SPACING.zero} ${SPACING.xxxs}`,
};
